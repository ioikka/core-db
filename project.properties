# ----------------------------------------------------------------
# !           Настройки исходной БД Oracle Финансов              !
# ----------------------------------------------------------------
#src.db.url=jdbc:oracle:thin:@172.21.10.103:1521:TORA1
#src.db.user=AZ_kraskray_41_230816
#src.db.password=AZ_kraskray_41_230816

src.db.url=jdbc:oracle:thin:@ip:1521:db
src.db.user=username
src.db.password=password

# ----------------------------------------------------------------
# !           Настройки БД Postgresql                            !
# ----------------------------------------------------------------
#dst.db.url=jdbc:postgresql://172.21.10.255:5432/fin_migrator
#dst.db.url=jdbc:postgresql://localhost:5432/fin_test_KK_0
#dst.db.user=postgres
#dst.db.password=toor

dst.db.url=jdbc:postgresql://localhost:5432/dbname
dst.db.user=user
dst.db.password=password

# ----------------------------------------------------------------
# !           Misc settings                                      !
# ----------------------------------------------------------------
#for repetitive tests and to save some loading time of metadata (сериализует метаданные в файл. Для удобства и экономии времени)
src.serialiaze.metadata=true

#создавать ли таблицы и  их структуры? (для миграции нет необходимости, т.к. скрипты штатно должны литься через DBUpdate) 
#dst.create.tables=false

#создавать ли fk констрейнты? (для миграции нет необходимости, т.к. скрипты штатно должны литься через DBUpdate)
#dst.create.fks=true

#создавать ли fk констрейнты? (для миграции нет необходимости, т.к. скрипты штатно должны литься через DBUpdate)
#dst.create.checks=true

#создавать ли sequence-ы? (есть смысл переносить оригинальные SEQUENCE-ы)
dst.create.sequences=true

#создавать ли вьюшки? (для миграции нет необходимости, т.к. скрипты штатно должны литься через DBUpdate)
#dst.create.views=true

#создавать ли уникальные ключи в таблицах? (для миграции нет необходимости, т.к. скрипты штатно должны литься через DBUpdate)
#dst.create.uqs=true

#конвертировать ли блобы из текста в бинарный формат? (конвертация необходима и должна проводиться один раз)
dst.convert.blobs=true

#dir to save csv data files. Куда записывать csv файлы. Пользователь должен предусмотреть достаточное кол-во свободной памяти на постоянном носителе типа жесткого диска) 
#example for Win 
#csv.dir=D:/share/csv/
#example for *nix     
csv.dir=/root/csv
#example for shared
#csv.dir=//ip/csv/


#tables data not to export (metadata is exported). Перечисление таблиц, данные которых не нужно переносить.
excluded.tables=DAYVERSION,LINKSTOCASCUPDATE,DBUPDATE
included.tables=DBUPDATE
#обработка только указанных в этом параметре таблиц. другие таблицы не будут ни экспортироваться, ни обрабатываться
#необходмо для тестирования (в частности OutOfMemory исключения)
#exclusive.tables=MSG

#перечисляем колонки, наименования которых должны быть в кавычках. Иначе эти колонки не везде будут проливаться 
dst.db.quoted.cols=LIMIT,modify,AS

#экспортировать ли данные в csv формате? (данные берутся из базы источника) 
export.to.csv=true

#импортировать ли данные из csv? (по идее, для PG импорт из CSV самый быстрый вариант переноса данных)
import.from.csv=true

